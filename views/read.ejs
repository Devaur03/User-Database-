<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div id="loader" class="fixed inset-0 flex items-center justify-center bg-zinc-900 z-50">
    <div class="w-16 h-16 border-4 border-blue-500 border-dashed rounded-full animate-spin"></div>
    </div>
    <div class="w-full min-h-screen bg-zinc-900 p-10">
        <div class="nav mb-10">
        <a class="bg-blue-500 text-white text-sm px-3 py-1 rounded hover:bg-blue-600" href="/">Home</a>
        </div>
        <h1 class="text-5xl text-white tracking-tighter mb-4">All users</h1>
        <div class="flex gap-2 mb-4">
            <a class="bg-gray-500 text-white text-sm px-3 py-1 rounded hover:bg-gray-600" href="/read?sort=name">Sort by Name</a>
            <a class="bg-gray-500 text-white text-sm px-3 py-1 rounded hover:bg-gray-600" href="/read?sort=email">Sort by Email</a>
        </div>
        <div class="users grid gap-6 grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4">
            <% if (users.length === 0) { %>
                <h1 class="text-3xl text-white tracking-tighter mb-4">No users found</h1>
            <% } %>
            <% users.forEach(function(user) { %>
                <div class="user w-72 p-4 bg-zinc-800 rounded-md mb-4 overflow-hidden transform transition-transform duration-300 hover:scale-105 hover:shadow-xl">
                    <div class="w-full h-72 rounded-md bg-zinc-700">
                        <img class="w-full h-full object-cover object-top" src="<%= user.image %>" alt="">
                    </div>
                        <h3 class="text-2xl text-white mt-4"><%= user.name %></h3>
                        <h5 class="text-sm text-zinc-400"><%= user.email %></h5>
                    <div class="flex gap-3 mt-10 items-center mt-4">
                        <a class="bg-blue-500 text-white px-3 py-1 rounded-md" href="/edit/<%= user._id %>">Edit</a>
                        <a class="bg-red-500 text-white px-3 py-1 rounded-md" href="/delete/<%= user._id %>" onclick="return confirm('Are you sure you want to delete this user?')">Delete</a>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
</body>
<script>
  window.addEventListener('load', () => {
    document.getElementById('loader').style.display = 'none';
  });
</script>
</html>
